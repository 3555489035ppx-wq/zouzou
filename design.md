# 走走 ZOUZOU · V6 Apple-aligned 设计基线

## 产品判断

走走面向正在和朋友一起构思、执行与复用旅行的年轻用户。核心闭环是：把一句旅行想法与若干截图，转换成一条能真正走完、可随时确认下一步、结束后还能回放与复用的完整路线。

## 视觉与结构

- 设备基准：402 × 874；同时覆盖 375 × 812 与 430 × 932。
- 设计类型：modern-minimal；核心宏观结构是 Map / Diagram。
- 色彩：纯白页面、近黑文字、冷中性灰、真实地图与自然照片。无米色、无彩色渐变、无发光、无装饰性玻璃。
- 字体：中文以系统原生圆体感为主，数字与状态使用同一无衬线体系；不混用超过两套字体。
- 层级：每屏至多一个黑色主按钮；页面标题、状态、正文和辅助信息形成清晰四级层级。
- 形态：16–24px 圆角用于卡片和底部面板；地图本身保持真实、克制，不模拟低多边形世界。
- 导航：四栏：首页、行程、社区、我。行程使用 Route 节点路径标志；社区使用闭合星球标志。导航条固定在安全区底部，页面内容预留等高空间，不覆盖长内容。

## 关键体验

- 首页：当前城市、用户头像、四张固定场景卡、动态行程模块和同城“你可能喜欢”推荐；城市切换后所有模块同步。
- 旅行创建：首屏完整容纳输入、截图横向轮播和主行动；截图支持滑动、删除、长按拖动排序。
- AI：同一个 Zou Bot 连续表达 Listening → Reading → Thinking → Planning → Done，状态和文案由统一服务驱动。
- 行程：真实路线底图，完整路线始终存在；已完成、当前、未来节点有明确差异；Bot 沿全路线移动；到站/下一站信息使用地图上的上下文卡而不是遮挡地图的大面板。
- 偏航：先询问用户是否调整，再局部重算。
- 社区回放：8–15 秒内完成全路线回放，地图收缩、圆角化并上移，连续过渡为详情页头图。
- 展示模式：`/__presentation` 使用 402 × 874 的真实比例 iPhone 展示壳，包含状态栏、灵动岛与 Home Indicator；正式应用内容从安全区后开始。
- 开发模式：`/__demo` 汇总路由和状态；`/__bot` 独立检查 Bot 状态。

## 动效

- UI 状态使用指数 ease-out；退出更快。
- Bot 使用纯时间采样和可中断状态过渡，不在页面散落定时器。
- 仅保留能解释状态、方向和进度的动效；减少动态模式降级为不超过 150ms 的淡入淡出或直接呈现终态。
- 拖放仅在释放时使用轻微物理回弹；按钮、面板与导航不使用弹跳。

## 设计例外

通用网页规则通常不建议手绘设备外框；本项目的 `/__presentation` 是任务书明确要求的独立演示工具，因此仅该路由使用设备展示壳。正式应用路由不绘制设备外框。

---

## V6 执行规范（Apple HIG 对齐）

### 设计目标

这是一个仅供本地私人使用的旅行规划与路线回放原型。所有界面优先服务“看懂当前状态、做出下一步、回到路线”的连续任务；不以装饰性动效或大面积卡片抢占内容。

### Apple 交互基线

- 安全区：展示壳的状态栏、灵动岛和 Home Indicator 独立于 iframe；应用内容从 72px 安全区开始，任何标题、头像和按钮不得进入状态栏。
- 导航：只保留四个顶层目的地，标签使用单词，选中态使用黑色与明确的图标变化；导航条固定在安全区底部，长页面预留等高空间，不覆盖内容。
- 触控：主要按钮、Tab、关闭和定位动作的可点击区域不少于 44px；图标旁必须有可读标签或 `aria-label`。
- 层级：一屏最多一个黑色主行动；次要行动使用描边或文本；正文、辅助说明、状态信息保持清晰对比。
- 面板：城市选择、评论和路线偏航使用单一底部 Sheet；Sheet 内滚动隐藏浏览器滚动条但保留触控滚动。
- 动效：启动页约 2 秒后进入首页；路线播放匀速、可暂停，不因开始播放自动改变镜头；减少动态效果时直接呈现终态。

### 状态与数据一致性

- `city` 是全局城市源；`setCity` 必须同时同步首页、社区、行程标题和同城推荐，禁止仅修改局部副本。
- `communityMapVisible` 只控制社区回放，设置文案必须明确“自己的行程地图始终保留”。
- `cover` 是用户封面源；我的页面封面可直接点击更换，选中后立即反馈并持久化。
- 行程 Tab 使用 Route 语义图标；选中时为黑色，未选中时为中性灰；不得退回普通小人或随机图标。

### 地图与 Bot

- 默认地图使用 Esri Canvas Light Gray Base + Reference 的浅色底图（配置 Key 时切换高德 JS API 2.0）；地图是路线上下文而不是整页背景，实时行程地图高度控制在约 360px，并保留今日进度与今日行程。
- 初次加载后调用 `resize`，容器尺寸变化不得造成拼贴、空白或地图不显示；路线首屏 Fit 全程，播放和“继续行程”不得强制局部放大。
- 路线 Bot 使用 Bloub SVG，周围保持透明；禁止黑色方形容器、贴图占位或与页面背景冲突的阴影。
- Bot 的眼神由当前交互目标驱动；行走状态保持匀速，节点到达用 arriving/wink 等明确状态表达，不以视频假播放。

### 首页与同城推荐

- 状态栏下的城市和天气是第一视觉锚点，顶部到灵动岛保持舒适间距。
- 私人原型默认显示稳定的 26°C 演示天气；实时天气必须通过显式 `VITE_LIVE_WEATHER=1` 开启，不能让网络波动改变验收截图。
- “为你推荐”说明创建场景；动态行程紧跟其后；底部空白区域用同城推荐列表填充，不用无意义留白。
- 同城推荐只显示当前城市的地点；点击后进入社区发现，不改变用户已保存的行程。

### 社区与我的

- 社区顶部层级：关注/发现、城市、搜索，再是推荐/旅行/周末/约会/聚餐；字号低于页面主标题，横向滚动不显示浏览器滚动条。
- 社区回放默认显示完整地图；关闭开关后用原内容封面和“社区地图已隐藏”说明替代，回放和详情仍可继续。
- 我的页面封面必须有真实图片或可替换的本地图片，不保留空白占位；点击封面直接打开图片选择器。
- 社区与我的长内容底部不穿透导航条；底部 Dock 固定在安全区并保留足够的内容底部空间。

### 回归流程（每次修改必跑）

1. 打开 `/`：确认走走启动页可见，约 2 秒自动进入 `/home`，无状态栏重叠。
2. 首页：选择城市（上海 → 杭州），确认首页城市、同城推荐、社区城市和行程标题同步；active 行程显示“Day 1 / 下一站”。
3. 点击“行程”：确认选中走路 Logo 为黑色，地图首屏全程且不突然放大；点击“继续行程”，Bot 匀速移动，进度与今日行程更新。
4. 点击“社区”：打开卡片，确认地图完整加载；设置中关闭社区地图后重新回放，确认只隐藏社区地图。
5. 点击“我”：向下滚动确认导航不覆盖内容；点击封面并选择本地图片，确认封面即时更新。
6. 在 320、375、402、430、768px 宽度检查无横向溢出；检查控制台无错误、键盘焦点可见、减少动态效果可用。

### 本轮截图问题 → 规范化修复

| 证据 | 问题 | 固化规则 |
|---|---|---|
| 图 1–2 | 行程图标姿态/选中颜色不一致 | 选中黑色行走姿态，未选中低对比静止姿态 |
| 图 3–4 | 顶部间距和首页下半段空白 | 安全区后统一节奏；动态行程后必须有同城推荐 |
| 图 5 | 城市选择只改局部 | `setCity` 同步全局城市源 |
| 图 6–7 | 地图过大、Bot 黑方形 | 360px 地图、首屏 Fit 全程、Bot 容器透明 |
| 图 8–9 | 社区地图拼贴/导航覆盖内容 | ResizeObserver；社区地图可隐藏；Dock 进入滚动流 |
| 图 10 | 我的封面缺少可编辑入口 | 封面整面可点击并持久化替换 |

---

## V7 最终固化规范（执行后）

### 产品入口与角色

- 根路由只展示一台 402 × 874 iPhone 17 Pro 展示壳：走走品牌启动页约 1.1 秒后进入登录；登录和资料引导完成后进入首页。
- 启动页只使用 `public/assets/brand/zouzou-walker.png`（从用户提供的走走源图裁切并去除白底）；不再绘制近似 Logo，也不在品牌图形上叠加眼睛、阴影或黑色方块。
- 三个视觉角色永久分离：启动页 Brand Walker、AI/路线 Bloub / Grok Bot、底部 Tab 的 Route 图标。行程 Tab 禁止使用普通人形图标。

### Safe Area 与导航

- 展示壳状态栏由外层统一绘制：`9:41`、图形 Signal/Wi‑Fi/Battery 和灵动岛；业务内容从 72px 安全区之后开始，禁止 Wi‑Fi 文案。
- 四项底部导航固定为：首页、行程、社区、我；高度约 72px、全宽、无浮动胶囊和透底照片。选中态黑色，未选中态中性灰，按下立即缩放反馈；所有长内容预留底栏空间。
- 社区详情的互动栏遵守同一宽度规则；按钮命中区不低于 44px，评论使用 Bottom Sheet。

### 首页与城市状态

- 首页纵向顺序固定为：城市/天气 →「为你推荐」→ 旅行/周末/约会/聚餐 → 当前行程状态 → 当前城市推荐。禁止用大块空白撑高度。
- `city` 是唯一城市源；选择杭州等城市后，首页、社区标题/Feed、行程标题、同城推荐和保存动作必须同步。已保存路线使用 `tripCity` 保持语义一致。
- 没有行程时显示创建态；保存后才显示进行中/即将开始卡片，卡片的下一站来自同一条路线数据。

### AI、方案与预算

- AI 理解阶段至少展示可感知的短过渡；“理解完成”和“开始规划”分离，最后一步只有方案生成后才完成。远程 AI 超过 1.5 秒自动回退本地解析，不阻塞用户。
- 三方案为可横滑卡片，保留约 8–12% peek，分页点与选择状态同步；方案详情默认只显示「替换」与「更多」操作。
- 住宿、餐饮、交通、门票、咖啡和缓冲均为真实参考价；用户界面只显示“最终价格与库存以第三方平台为准”，禁止出现开发执行器或本地 Demo 文案。

### 地图、路线与 Bot

- 地图适配器优先高德 JS API 2.0 浅色样式；加载失败或无 Key 时使用稳定的本地浅色矢量底图，不出现灰/白空地图。方案详情、行程和社区回放共享同一地图语言。
- 首屏 Fit 全路线；点击继续行程只在当前段开始时 Fit 当前/下一站一次，播放期间不持续缩放、抖动或重绘镜头。地图高度约 360px，并在其下显示今日进度与今日行程。
- 路线 Bot 保持透明容器，状态在 walking/listening/done/arriving/paused 间平滑切换，禁止思考态独占路线播放；每段约 2.5–8 秒并支持减少动态效果。
- 社区地图开关只影响社区回放；关闭后仍可读详情和具体行程，自己的行程地图永远保留。

### 社区与我的

- 社区采用双列内容流：关注/发现、城市、分类；卡片直接进入详情，不强制播放路线。详情包含作者、正文、数据摘要和具体行程，路线回放为显式可选 CTA。
- Like 为红色 filled Heart 并即时更新数量；Favorite 为黑色 filled Bookmark；Comment 为可发送/回复的 Bottom Sheet；Follow 为可撤销的即时状态。
- 我的页面 Cover 从内容顶部 Full Bleed 开始，整张封面可点击替换；行程、发布、收藏、喜欢网格全部可进入对应详情，不强制回放。

### 每次提交的真实用户回归

从刷新开始走完整链路：启动 → 登录 → 首页 → 城市 → 旅行输入/截图 → AI 理解 → 三方案 → 详情/Day 2/地图/替换/预算 → 朋友/投票/保存 → 首页 → 行程/继续/地图/到达/Day 2 → 社区 Feed/详情/点赞/收藏/评论/回放/使用行程 → 我的/网格/编辑资料/设置。必须同时检查 320、375、402、430、768px 无横向溢出、无控制台错误、减少动态效果和底部安全区。
