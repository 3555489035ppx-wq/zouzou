# 走走｜上线缺口报告

## P0

核心旅行创建链路、三方案生成、确认/开始/完成状态、运行时校验、AI 本地回退、真实 POI/路线边界、地图超时降级、Safe Area、Reduce Motion 和基础回归测试已落地。

## P1

Group Plan 已有本地 SQLite + REST/SSE + 邀请/参与者/投票完整开发边界；生产还缺账号鉴权、云数据库、跨实例 realtime、通知、冲突策略和权限模型。

社区评论已能本地持久化；发布、收藏、关注、点赞和评论的云同步、内容审核、举报和分页仍缺生产后端。

AI 已有 BFF 和结构化响应/回退；生产还缺正式 Provider 配额、重试/熔断、审计、成本监控和更严格的 prompt 注入防护。

## P2/P3

OCR 离线包、离线地图、PWA 安装、拖拽排序、Yjs 实时协作、真实 3D 城市、原生 Haptic、商店包、订单/库存和真实天气/票务供应商不纳入本轮完成承诺。

## 发布门槛

在获得正式地图限制 Key、AI Key、账号/数据库/对象存储方案和隐私合规确认前，不部署、不声称上线级生产服务。
